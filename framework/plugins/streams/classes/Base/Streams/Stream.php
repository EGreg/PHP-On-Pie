<?php

/**
 * Autogenerated base class representing stream rows
 * in the streams database.
 * 
 * Don't change this file, since it can be overwritten.
 * Instead, change the Streams_Stream.php file.
 *
 * @package streams
 *
 * @property int $publisher_id
 * @property string $time_created
 * @property string $name
 * @property string $alias_of_name
 * @property string $type
 * @property string $title
 * @property string $icon
 * @property string $content
 */
abstract class Base_Streams_Stream extends Db_Row
{
	function setUp()
	{
		$this->setDb(self::db());
		$this->setTable(self::table());
		$this->setPrimaryKey(
			array (
			  0 => 'publisher_id',
			  1 => 'name',
			)
		);
	}

	static function db()
	{
		return Db::connect('streams');
	}

	static function table($with_db_name = true)
	{
		$conn = Db::getConnection('streams');
		$prefix = empty($conn['prefix']) ? '' : $conn['prefix'];
		$table_name = $prefix . 'stream';
		if (!$with_db_name)
			return $table_name;
		$db = Db::connect('streams');
		return $db->dbName().'.'.$table_name;
	}
	
	static function connectionName()
	{
		return 'streams';
	}

	/** @return Db_Query_Mysql */
	static function select($fields, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->select($fields, self::table().' '.$alias);
		$q->className = 'Streams_Stream';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function update($alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->update(self::table().' '.$alias);
		$q->className = 'Streams_Stream';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function delete($table_using = null, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->delete(self::table().' '.$alias, $table_using);
		$q->className = 'Streams_Stream';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insert($fields = array(), $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insert(self::table().' '.$alias, $fields);
		$q->className = 'Streams_Stream';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insertManyAndExecute($records = array(), $chunk_size = 1, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insertManyAndExecute(self::table().' '.$alias, $records, $chunk_size);
		$q->className = 'Streams_Stream';
		return $q;
	}
	
	function beforeSet_publisher_id($value)
	{
		if ($value instanceof Db_Expression) return array('publisher_id', $value);
		if (!is_numeric($value) or floor($value) != $value)
			throw new Exception('Non-integer value being assigned to streams_stream.publisher_id');
		if ($value < 0 or $value > 1.844674407371E+19)
			throw new Exception('Out-of-range value being assigned to streams_stream.publisher_id');
		return array('publisher_id', $value);			
	}

	function beforeSet_name($value)
	{
		if ($value instanceof Db_Expression) return array('name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_stream.name');
		return array('name', $value);			
	}

	function beforeSet_alias_of_name($value)
	{
		if ($value instanceof Db_Expression) return array('alias_of_name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.alias_of_name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_stream.alias_of_name');
		return array('alias_of_name', $value);			
	}

	function beforeSet_type($value)
	{
		if ($value instanceof Db_Expression) return array('type', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.type');
		if (strlen($value) > 63)
			throw new Exception('Exceedingly long value being assigned to streams_stream.type');
		return array('type', $value);			
	}

	function beforeSet_title($value)
	{
		if ($value instanceof Db_Expression) return array('title', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.title');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_stream.title');
		return array('title', $value);			
	}

	function beforeSet_icon($value)
	{
		if ($value instanceof Db_Expression) return array('icon', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.icon');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_stream.icon');
		return array('icon', $value);			
	}

	function beforeSet_content($value)
	{
		if ($value instanceof Db_Expression) return array('content', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_stream.content');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_stream.content');
		return array('content', $value);			
	}

	function afterSet($name, $value)
	{
		if (!in_array($name, $this->fieldNames()))
			$this->notModified($name);
		return $value;			
	}

	function beforeSave($value)
	{
		if (!$this->retrieved) {
			foreach (array('publisher_id') as $name) {
				if (!isset($value[$name]))
					throw new Exception("The field streams_stream.$name needs a value, because it is NOT NULL, not auto_increment, and lacks a default value.");
			}
		}
		return $value;			
	}

	function fieldNames($table_alias = null, $field_alias_prefix = null)
	{
		$field_names = array('publisher_id', 'time_created', 'name', 'alias_of_name', 'type', 'title', 'icon', 'content');
		$result = $field_names;
		if (!empty($table_alias)) {
			$temp = array();
			foreach ($result as $field_name)
				$temp[] = $table_alias . '.' . $field_name;
			$result = $temp;
		} 
		if (!empty($field_alias_prefix)) {
			$temp = array();
			reset($field_names);
			foreach ($result as $field_name) {
				$temp[$field_alias_prefix . current($field_names)] = $field_name;
				next($field_names);
			}
			$result = $temp;
		}
		return $result;			
	}
};