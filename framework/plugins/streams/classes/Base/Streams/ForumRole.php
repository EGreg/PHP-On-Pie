<?php

/**
 * Autogenerated base class representing forum_role rows
 * in the streams database.
 * 
 * Don't change this file, since it can be overwritten.
 * Instead, change the Streams_ForumRole.php file.
 *
 * @package streams
 *
 * @property int $publisher_id
 * @property string $forum_name
 * @property int $of_user_id
 * @property string $time_created
 * @property int $level
 */
abstract class Base_Streams_ForumRole extends Db_Row
{
	function setUp()
	{
		$this->setDb(self::db());
		$this->setTable(self::table());
		$this->setPrimaryKey(
			array (
			  0 => 'publisher_id',
			  1 => 'forum_name',
			  2 => 'of_user_id',
			)
		);
	}

	static function db()
	{
		return Db::connect('streams');
	}

	static function table($with_db_name = true)
	{
		$conn = Db::getConnection('streams');
		$prefix = empty($conn['prefix']) ? '' : $conn['prefix'];
		$table_name = $prefix . 'forum_role';
		if (!$with_db_name)
			return $table_name;
		$db = Db::connect('streams');
		return $db->dbName().'.'.$table_name;
	}
	
	static function connectionName()
	{
		return 'streams';
	}

	/** @return Db_Query_Mysql */
	static function select($fields, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->select($fields, self::table().' '.$alias);
		$q->className = 'Streams_ForumRole';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function update($alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->update(self::table().' '.$alias);
		$q->className = 'Streams_ForumRole';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function delete($table_using = null, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->delete(self::table().' '.$alias, $table_using);
		$q->className = 'Streams_ForumRole';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insert($fields = array(), $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insert(self::table().' '.$alias, $fields);
		$q->className = 'Streams_ForumRole';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insertManyAndExecute($records = array(), $chunk_size = 1, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insertManyAndExecute(self::table().' '.$alias, $records, $chunk_size);
		$q->className = 'Streams_ForumRole';
		return $q;
	}
	
	function beforeSet_publisher_id($value)
	{
		if ($value instanceof Db_Expression) return array('publisher_id', $value);
		if (!is_numeric($value) or floor($value) != $value)
			throw new Exception('Non-integer value being assigned to streams_forum_role.publisher_id');
		if ($value < 0 or $value > 1.844674407371E+19)
			throw new Exception('Out-of-range value being assigned to streams_forum_role.publisher_id');
		return array('publisher_id', $value);			
	}

	function beforeSet_forum_name($value)
	{
		if ($value instanceof Db_Expression) return array('forum_name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to streams_forum_role.forum_name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to streams_forum_role.forum_name');
		return array('forum_name', $value);			
	}

	function beforeSet_of_user_id($value)
	{
		if ($value instanceof Db_Expression) return array('of_user_id', $value);
		if (!is_numeric($value) or floor($value) != $value)
			throw new Exception('Non-integer value being assigned to streams_forum_role.of_user_id');
		if ($value < 0 or $value > 1.844674407371E+19)
			throw new Exception('Out-of-range value being assigned to streams_forum_role.of_user_id');
		return array('of_user_id', $value);			
	}

	function beforeSet_level($value)
	{
		if ($value instanceof Db_Expression) return array('level', $value);
		if (!is_numeric($value) or floor($value) != $value)
			throw new Exception('Non-integer value being assigned to streams_forum_role.level');
		if ($value < -2147483648 or $value > 2147483647)
			throw new Exception('Out-of-range value being assigned to streams_forum_role.level');
		return array('level', $value);			
	}

	function afterSet($name, $value)
	{
		if (!in_array($name, $this->fieldNames()))
			$this->notModified($name);
		return $value;			
	}

	function beforeSave($value)
	{
		if (!$this->retrieved) {
			foreach (array('publisher_id','of_user_id','level') as $name) {
				if (!isset($value[$name]))
					throw new Exception("The field streams_forum_role.$name needs a value, because it is NOT NULL, not auto_increment, and lacks a default value.");
			}
		}
		return $value;			
	}

	function fieldNames($table_alias = null, $field_alias_prefix = null)
	{
		$field_names = array('publisher_id', 'forum_name', 'of_user_id', 'time_created', 'level');
		$result = $field_names;
		if (!empty($table_alias)) {
			$temp = array();
			foreach ($result as $field_name)
				$temp[] = $table_alias . '.' . $field_name;
			$result = $temp;
		} 
		if (!empty($field_alias_prefix)) {
			$temp = array();
			reset($field_names);
			foreach ($result as $field_name) {
				$temp[$field_alias_prefix . current($field_names)] = $field_name;
				next($field_names);
			}
			$result = $temp;
		}
		return $result;			
	}
};