<?php

/**
 * Autogenerated base class representing tag rows
 * in the items database.
 * 
 * Don't change this file, since it can be overwritten.
 * Instead, change the Items_Tag.php file.
 *
 * @package items
 *
 * @property int $publisher_id
 * @property string $item_name
 * @property string $attribute_name
 * @property string $category_name
 * @property float $weight
 */
abstract class Base_Items_Tag extends Db_Row
{
	function setUp()
	{
		$this->setDb(self::db());
		$this->setTable(self::table());
		$this->setPrimaryKey(
			array (
			  0 => 'publisher_id',
			  1 => 'item_name',
			  2 => 'attribute_name',
			  3 => 'category_name',
			)
		);
	}

	static function db()
	{
		return Db::connect('items');
	}

	static function table($with_db_name = true)
	{
		$conn = Db::getConnection('items');
		$prefix = empty($conn['prefix']) ? '' : $conn['prefix'];
		$table_name = $prefix . 'tag';
		if (!$with_db_name)
			return $table_name;
		$db = Db::connect('items');
		return $db->dbName().'.'.$table_name;
	}
	
	static function connectionName()
	{
		return 'items';
	}

	/** @return Db_Query_Mysql */
	static function select($fields, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->select($fields, self::table().' '.$alias);
		$q->className = 'Items_Tag';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function update($alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->update(self::table().' '.$alias);
		$q->className = 'Items_Tag';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function delete($table_using = null, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->delete(self::table().' '.$alias, $table_using);
		$q->className = 'Items_Tag';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insert($fields = array(), $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insert(self::table().' '.$alias, $fields);
		$q->className = 'Items_Tag';
		return $q;
	}

	/** @return Db_Query_Mysql */
	static function insertManyAndExecute($records = array(), $chunk_size = 1, $alias = null)
	{
		if (!isset($alias)) $alias = '';
		$q = self::db()->insertManyAndExecute(self::table().' '.$alias, $records, $chunk_size);
		$q->className = 'Items_Tag';
		return $q;
	}
	
	function beforeSet_publisher_id($value)
	{
		if ($value instanceof Db_Expression) return array('publisher_id', $value);
		if (!is_numeric($value) or floor($value) != $value)
			throw new Exception('Non-integer value being assigned to items_tag.publisher_id');
		if ($value < 0 or $value > 1.844674407371E+19)
			throw new Exception('Out-of-range value being assigned to items_tag.publisher_id');
		return array('publisher_id', $value);			
	}

	function beforeSet_item_name($value)
	{
		if ($value instanceof Db_Expression) return array('item_name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to items_tag.item_name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to items_tag.item_name');
		return array('item_name', $value);			
	}

	function beforeSet_attribute_name($value)
	{
		if ($value instanceof Db_Expression) return array('attribute_name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to items_tag.attribute_name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to items_tag.attribute_name');
		return array('attribute_name', $value);			
	}

	function beforeSet_category_name($value)
	{
		if ($value instanceof Db_Expression) return array('category_name', $value);
		if (!is_string($value))
			throw new Exception('Must pass a string to items_tag.category_name');
		if (strlen($value) > 255)
			throw new Exception('Exceedingly long value being assigned to items_tag.category_name');
		return array('category_name', $value);			
	}

	function afterSet($name, $value)
	{
		if (!in_array($name, $this->fieldNames()))
			$this->notModified($name);
		return $value;			
	}

	function beforeSave($value)
	{
		if (!$this->retrieved) {
			foreach (array('publisher_id') as $name) {
				if (!isset($value[$name]))
					throw new Exception("The field items_tag.$name needs a value, because it is NOT NULL, not auto_increment, and lacks a default value.");
			}
		}
		return $value;			
	}

	function fieldNames($table_alias = null, $field_alias_prefix = null)
	{
		$field_names = array('publisher_id', 'item_name', 'attribute_name', 'category_name', 'weight');
		$result = $field_names;
		if (!empty($table_alias)) {
			$temp = array();
			foreach ($result as $field_name)
				$temp[] = $table_alias . '.' . $field_name;
			$result = $temp;
		} 
		if (!empty($field_alias_prefix)) {
			$temp = array();
			reset($field_names);
			foreach ($result as $field_name) {
				$temp[$field_alias_prefix . current($field_names)] = $field_name;
				next($field_names);
			}
			$result = $temp;
		}
		return $result;			
	}
};